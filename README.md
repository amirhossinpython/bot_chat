# 🤖 ربات روبیکا - MirBot

این پروژه یک **ربات هوش مصنوعی برای روبیکا** هست که توسط امیرحسین خزاعی ساخته شده.  
ربات با اتصال به مدل‌های مختلف هوش مصنوعی (GPT4 ) به شما پاسخ می‌دهد و کاملاً قابل شخصی‌سازی است.

---

## ✨ ویژگی‌ها
- ثبت و مدیریت کاربران در دیتابیس SQLite  
- ذخیره تاریخچه پیام‌ها، درخواست‌ها و لاگ‌ها  
- پشتیبانی از چت خصوصی و گروهی  
- محدودیت زمانی برای جلوگیری از اسپم  
- امکان تقسیم پاسخ‌های طولانی به چند بخش  
- شخصی‌سازی پرامپت اصلی در متغیر `system_prompt`  
- ارائه آمار کامل کاربران و پیام‌ها  

---


### ۲. نصب وابستگی‌ها
```bash
pip install -r requirements.txt
```

### ۳. تنظیم توکن ربات
در فایل `bot.py` مقدار زیر را با توکن خودتان جایگزین کنید:
```python
bot = BotClient(token="توکن_ربات_خود")
```

### ۴. اجرای ربات
```bash
python bot.py
```

---

## 🛠️ دیتابیس
این ربات از **SQLite** استفاده می‌کند و سه جدول اصلی دارد:
- **users** → اطلاعات کاربران  
- **requests** → پیام‌ها و پاسخ‌ها  
- **logs** → تاریخچه لاگ‌ها  

---

## 🧑‍💻 شخصی‌سازی پرامپت
در متغیر `system_prompt` می‌توانید پرامپت دلخواه خود را قرار دهید تا ربات بر اساس آن رفتار کند.  

نمونه:
```python
system_prompt = """
تویک ربات مسلمانی
"""
```

---

## 📊 دستورات
- `/start` یا `/help` → شروع و مشاهده قوانین  
- `/آمار` → نمایش آمار شخصی و کل ربات  

---

## 📌 نکات مهم
- برای جلوگیری از اسپم، هر کاربر فقط می‌تواند هر **۱۰ ثانیه** یک درخواست ارسال کند.  
- پاسخ‌ها در صورت طولانی بودن به صورت چندبخشی ارسال می‌شوند.  

---

## 🙏 سازنده
این ربات با عشق توسط **امیرحسین خزاعی** ساخته شده ❤️  
